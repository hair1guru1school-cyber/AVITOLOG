Структура AVITOBO$$ (разбиение для стабильности Cursor)
=======================================================

index.html (~1670 строк)
  - HTML-разметка, inline-стили, подключение скриптов
  
js/
  core.js     (~1393 строк) — API, KP, режимы, вкладки, Agency, Strategy
  projects.js (~2983 строк) — экран ПРОЕКТЫ, календарь, задачи
  main.js     (~3349 строк) — аналитика, Drive, CRM, генерация, init
  goals.js    — модуль CRM (цели)
  app.js      — устаревший монолит (игнорируется Cursor)

Порядок загрузки: core.js → projects.js → main.js
(app.js оставлен как резервная копия, не используется)
